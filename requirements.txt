# AnalytIQ Agent — Python dependencies

# Core
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0

# Statistical modeling
statsmodels>=0.14.0
scikit-learn>=1.4.0
matplotlib<3.8.0
plotly>=5.18.0

# AutoML
pycaret[full]>=3.3.0

# LLM backends
anthropic>=0.28.0           # Claude API (tool use + ReAct agent)
google-generativeai>=0.7.0  # Gemini API (vision + long context)
openai>=1.30.0              # Groq + LM Studio (both OpenAI-compatible)
pillow>=10.0.0              # Required by Gemini vision (image loading)
ollama>=0.2.0               # Only needed if LLM_BACKEND=ollama

# Report generation
jinja2>=3.1.0
pyarrow>=15.0.0

# Utilities
openpyxl>=3.1.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
