---
title: "AnalytIQ Agent — Automated Statistical Modeling Report"
subtitle: "{{ analysis_goal }}"
date: "{{ report_date }}"
format:
  html:
    toc: true
    toc-depth: 3
    theme: cosmo
    self-contained: true
    code-fold: true
  pdf:
    toc: true
    geometry: margin=1in
    fontsize: 11pt
---

```{python}
#| echo: false
import json, os
data = json.loads(open("{{ data_path }}").read())
```

# 1. Analysis Goal

**{{ analysis_goal }}**

- **Outcome variable:** `{{ outcome }}`
- **Analysis type:** {{ goal_type }}
- **Date:** {{ report_date }}

---

# 2. Data Quality Summary

{{ data_quality_summary }}

**Dataset shape:** {{ n_rows }} rows × {{ n_cols }} columns.

| Issue | Count |
|---|---|
| Missing value columns | {{ missing_cols }} |
| Duplicate rows | {{ duplicate_rows }} |
| High-cardinality columns | {{ high_cardinality_cols }} |

---

# 3. EDA Recommendations

The following visualisations are recommended before modeling:

{{ eda_summary }}

---

# 4. Model Recommendations

Based on the outcome type (`{{ outcome_type }}`), the following models were considered:

{{ model_recommendations }}

---

# 5. Fitted Model

**Selected model:** {{ best_model_type }}

**Formula:** `{{ formula }}`

**Engine:** {{ engine }}

```
{{ model_summary_text }}
```

**Key metrics:**

{{ metrics_table }}

---

# 6. Diagnostics

{{ diagnostics_summary }}

{% if threshold_section %}
## 6a. Threshold Tuning (Logistic Regression)

{{ threshold_summary }}

| Criterion | Optimal Threshold | Sensitivity | Specificity | F1 |
|---|---|---|---|---|
| Youden's J | {{ youden_threshold }} | {{ youden_sens }} | {{ youden_spec }} | — |
| F1-maximising | {{ f1_threshold }} | {{ f1_recall }} | — | {{ f1_score }} |

{% endif %}

---

# 7. Model Comparison

{% if comparison_table %}

{{ comparison_notes }}

{{ comparison_table_md }}

{% else %}
Only one model was evaluated.
{% endif %}

---

# 8. Plain-English Interpretation

{{ plain_english_summary }}

---

# 9. Revisions & Warnings

{% if revisions %}
The agent detected the following issues during analysis and revised its approach:

{% for revision in revisions %}
- {{ revision }}
{% endfor %}

{% else %}
No major revisions were required during this analysis.
{% endif %}

---

{% if dynamic_used %}
# 10. Dynamic Analysis Note

This report includes a **dynamically generated analysis** for a method not available
in the standard tool registry. The generated code was safety-validated before
execution. Review the generated code in the `sandbox/` directory for full
reproducibility.

{% endif %}

# 11. Reproducibility

- **AnalytIQ Agent version:** {{ agent_version }}
- **Python packages:** statsmodels, scikit-learn, pandas, numpy
- **R packages (if used):** tidyverse, broom, car, emmeans
- **Random seed:** 42
- **Report generated:** {{ report_date }}

---

*This report was generated by AnalytIQ Agent. Statistical calculations are deterministic
and reproducible. Plain-English summaries may have been assisted by a local LLM.*
